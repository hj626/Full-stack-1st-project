<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE configuration
PUBLIC "-/mybatis.org//DTD Config 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>

	<!-- 만약 프로젝트에서 spring만이 아닌 여러 프레임워크를 사용한다면 servlet-context를 통해 DB를 연결하는 방식을
	 이용할 수 없을 수도 있으므로 그런 경우엔 이 방법을 통해 DB를 연결해야 한다. -->
	<!-- 
	<properties>
		<property name="driver" value="oracle.jdbc.driver.OracleDriver"/>
		<property name="url" value="jdbc:oracle:thin:@192.168.0.76:1521:xe"/>
		<property name="username" value="suzi"/>
		<property name="password" value="a123"/>
	</properties>

	<environments default="development">
		<environment id="development">
			<transactionManager type="JDBC"/>
			<dataSource type="POOLED">
				<property name="driver" value="${driver}"/>
				<property name="url" value="${url}"/>
				<property name="username" value="${username}"/>
				<property name="password" value="${password}"/>
			</dataSource>
		</environment>
	</environments>
	 -->


	<mappers>
		<mapper resource="com/fundee/mapper/NoticeMapper.xml"/>
		<mapper resource="com/fundee/mapper/AdminMapper.xml"/>
		<mapper resource="com/fundee/mapper/PostsMapper.xml"/>
		<mapper resource="com/fundee/mapper/LoginMapper.xml"/>
		<mapper resource="com/fundee/mapper/BuyMapper.xml"/>
	</mappers>
	
	
	
</configuration>